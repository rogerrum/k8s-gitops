botkube:
  communications:
    'default-group':
      discord:
        token: '{{ .botkube_discord_token }}'
        botID: '{{ .botkube_discord_botId }}'
        channels:
          'default':
            id: '{{ .botkube_discord_channelId }}'
