thanos:
  objstoreConfig: |-
    type: s3
    config:
      bucket: thanos
      endpoint: "minio.default.svc:9000"
      access_key: "{{ .THANOS_ACCESS_KEY }}"
      secret_key: "{{ .THANOS_SECRET_KEY }}"
      insecure: true
