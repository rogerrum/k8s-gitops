step-issuer:
  stepIssuer:
    create: true
    caBundle: {{ .caBundle | base64encode }}
    provisioner:
      name: {{ .provisioner }}
      kid: {{ .kid }}

  stepClusterIssuer:
    create: true
    caBundle: {{ .caBundle | base64encode }}
    provisioner:
      name: {{ .provisioner }}
      kid: {{ .kid }}
